peko update

Re-resolves the project's dependencies and refreshes peko.lock.

Ignores the existing lockfile and selects the highest versions that satisfy
the [dependencies] table in peko.toml, downloads them, and rewrites peko.lock.

EXAMPLES
    peko update
